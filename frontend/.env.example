# Copy to .env.local and set your backend URL.
# Backend must be running (e.g. uvicorn app.main:app --reload --port 8000 in Hackathon2026).
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Google OAuth Client ID for client-side Google Docs export (GCP Console → Credentials → OAuth 2.0 Client ID)
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=116980611136-v4lr9ifm5j2bd0t99nk9hmqrbi18gdre.apps.googleusercontent.com
